/**
 * Barra de CTA (IR AGORA) acima da navegação inferior.
 * A bottom nav ocupa ~4.75rem + safe area.
 */
export const STICKY_CTA_ABOVE_NAV_CLASS =
  "fixed inset-x-0 z-50 bottom-[calc(4.75rem+env(safe-area-inset-bottom,0px))]";

/** Padding inferior em páginas de detalhe com CTA + bottom nav. */
export const DETALHE_PAGE_PAD_WITH_CTA = "pb-44";
