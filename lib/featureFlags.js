/**
 * Flags locais de funcionalidades isoladas.
 *
 * Para desativar um card da home (ou o hub de condições), altere a flag
 * correspondente para `false` — sem afetar clima, lugares ou navegação.
 */

/** Card + rota `/condicoes` (home e hub de esportes aquáticos). */
export const CONDICOES_HUB_ENABLED = true;

/** Card sazonal "Onde tem baleia?" na home. */
export const BALEIAS_CARD_ENABLED = true;
