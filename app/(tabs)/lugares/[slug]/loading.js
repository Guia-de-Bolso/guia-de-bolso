import DetalhePageSkeleton from "@/components/loading/DetalhePageSkeleton";

/**
 * Skeleton do detalhe do lugar enquanto o segmento de rota carrega.
 * @returns {import("react").ReactElement}
 */
export default function LugarDetalheLoading() {
  return <DetalhePageSkeleton />;
}
