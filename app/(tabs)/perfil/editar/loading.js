import PerfilPageSkeleton from "@/components/loading/PerfilPageSkeleton";

/**
 * Skeleton do editar perfil enquanto o segmento carrega.
 * @returns {import("react").ReactElement}
 */
export default function PerfilEditarLoading() {
  return <PerfilPageSkeleton />;
}
