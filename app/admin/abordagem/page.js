import AbordagemComercialPage from "@/components/admin/AbordagemComercialPage";

/**
 * Admin — fila de abordagem comercial (dados reais → cobrança).
 * @returns {import("react").JSX.Element}
 */
export default function AdminAbordagemPage() {
  return <AbordagemComercialPage />;
}
